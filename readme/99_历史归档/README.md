# 历史文档归档

此目录保存旧版本升级说明、阶段测试报告、旧目录路径和历史限制。

注意：

- 历史文件没有被改写，便于追溯当时的实现与测试证据。
- 其中的模型名、参数范围、工作流数量、插件状态和绝对路径可能已经过期。
- 当前使用应从 `readme/00_文档索引.md` 进入，并以当前代码、Manifest、JSON Schema 和自动检查结果为准。
- `版本快照/` 保存产品升级原文。
- `ComfyUI阶段报告/` 保存 ComfyUI v2.5.0–v2.5.5 的演进记录。

