> **核心逻辑**：前端统一用标准化参数（resolution、duration、mode 等）→ `video_param_mapper.js` 自动转换 → 发送前自动剥离不兼容字段

---

## 一、Veo 系列（Google）

### 🟢 veo3.1 / veo3.1-4k（标准/高质量版）

| 前端配置列 (UI) | 实际传给 API | 状态 | 解读 |
|----------------|-------------|------|------|
| 分辨率 (resolution) | ❌ 被删除 | 🚫 不起效 | 画质由模型版本决定，传了会报错 |
| 生成模式 (mode) | `generation_mode` | ✅ 起效 | quality→pro, fast→fast, standard→"null" |
| 画面比例 (aspect_ratio) | `aspect_ratio` | ✅ 起效 | 16:9 / 9:16 原样传递 |
| 时长 (duration) | ❌ 被删除 | 🚫 不起效 | Veo 不支持自定时长，自动拦截 |
| 提示词优化 (enhance_prompt) | `enhance_prompt: "true/false"` | ✅ 起效 | 布尔值转字符串 |
| 视频超分 (enable_upsample) | `enable_upsample` | ✅ 起效 | 隐藏参数，支持则传递 |

### 🔵 veo3.1-lite（轻量版）

| 前端配置列 (UI) | 实际传给 API | 状态 | 解读 |
|----------------|-------------|------|------|
| 分辨率 (resolution) | `quality: "sd/4k"` | ✅ 起效 | 4K→"4k", 720P/1080P→"sd" |
| 生成模式 (mode) | ❌ 被删除 | 🚫 不起效 | lite 版不支持生成模式 |
| 画面比例 (aspect_ratio) | `aspect_ratio` | ✅ 起效 | 原样传递 |
| 时长 (duration) | ❌ 被删除 | 🚫 不起效 | lite 版不支持时长 |
| 提示词优化 (enhance_prompt) | `enhance_prompt` | ✅ 起效 | 布尔转字符串 |
| 视频超分 (enable_upsample) | ❌ 被删除 | 🚫 不起效 | lite 版不支持此功能 |

---

## 二、Grok 系列（xAI）

### 🟣 grok-video-3（标准版）

| 前端配置列 (UI) | 实际传给 API | 状态 | 解读 |
|----------------|-------------|------|------|
| 画质 (resolution) | `size: "720P"` | ✅ 起效 | Grok 把分辨率叫 size，仅支持 720P |
| 画面比例 (aspect_ratio) | `aspect_ratio` | ✅ 起效 | 支持 16:9 / 9:16 / 2:3 / 3:2 / 1:1 |
| 视频时长 (duration) | `duration` | ✅ 起效 | 仅 6s 或 10s 两档。映射规则：≤6 → `"6"`，≥7 → `"10"` |
| 首帧参考图 (images) | `images` | ✅ 起效 | 可选传 1 张图作为首帧，不传=文生视频 |
| 生成模式 (mode) | ❌ 被删除 | 🚫 不起效 | Grok 不认生成模式，自动剥离 |
| 音效 (sound) | ❌ 被删除 | 🚫 不起效 | grok-video-3 已不支持音效参数 |

### 🆕 grok-imagine-video-1.5-preview（Imagine 1.5）

**说明**：xAI 官方 Imagine 1.5 视频模型，专注图生视频，内置音频，1-15s 灵活时长。

> ⚠️ 仅支持图生视频，**必须**传入首帧参考图

| 前端配置列 (UI) | 实际传给 API | 状态 | 解读 |
|----------------|-------------|------|------|
| 画质 (resolution) | `resolution` | ✅ 起效 | 720p 或 480p |
| 画面比例 (aspect_ratio) | `aspect_ratio` | ✅ 起效 | 16:9 / 9:16 / 1:1 / 3:2 / 2:3 |
| 视频时长 (duration) | `duration` | ✅ 起效 | 1-15s，按秒计费 |
| 首帧参考图 (images) | `images` | ✅ 必填 | 必须传 1 张图作为首帧 |
| 生成模式 (mode) | ❌ 被删除 | 🚫 不起效 | 不支持生成模式 |
| 音效 (sound) | ✅ 内置 | ✅ 自带 | 模型自动生成音频，无需额外参数 |

### ⚪ grok-video-3-plus / grok-videos（已下线）

> ⚠️ 平台已不再提供 `grok-video-3-plus` 和 `grok-videos` 模型，请迁移至 `grok-video-3` 或 `grok-imagine-video-1.5-preview`。

---

## 三、一图看懂参数流向

```
┌─────────────────────────────────────────────────────────────────────┐
│                        前端统一输入                                  │
│   resolution  duration  mode  aspect_ratio  enhance_prompt  sound   │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│                   video_param_mapper.js 映射脚本                      │
│  ① 根据模型版本转换字段名  ② 自动 delete 不兼容参数                  │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
         ┌────────────────────────┼────────────────────────┐
         ▼                        ▼                        ▼
┌───────────────┐        ┌───────────────┐        ┌───────────────┐
│    Veo 系列    │        │ grok-video-3  │        │ Imagine 1.5   │
├───────────────┤        ├───────────────┤        ├───────────────┤
│ generation_   │        │ size          │        │ resolution    │
│ mode / quality│        │ duration      │        │ duration      │
│ aspect_ratio  │        │ aspect_ratio  │        │ aspect_ratio  │
└───────────────┘        └───────────────┘        └───────────────┘
```

---

## 四、常见报错速查表

| 报错信息 | 可能原因 | 已修复方案 |
|---------|---------|-----------|
| 未定义的字段: generation_mode | 给 Grok 传了 mode | ✅ 自动剥离 |
| 未知的参数: quality | 给 Veo 标准版传了 resolution | ✅ 自动剥离 |
| 参数验证失败: size | 给 Imagine 1.5 传了 size | ✅ 自动转换/剥离 |
| 不支持的参数: generate_audio | 给 grok-video-3 传了 sound | ✅ 自动剥离 |

---

## 五、推荐配置组合

### 🎯 Veo 系列推荐
| 场景 | 模型选择 | 推荐参数 |
|------|---------|---------|
| 高质量视频 | veo3.1-4k | mode=quality, 比例=16:9 |
| 快速出片 | veo3.1-lite | resolution=4K, 比例=16:9 |

### 🎯 Grok 系列推荐
| 场景 | 模型选择 | 推荐参数 |
|------|---------|---------|
| 标准视频 | grok-video-3 | resolution=720P, duration=10, 比例=16:9 |
| 图生视频(带音效) | grok-imagine-video-1.5-preview | resolution=720P, duration=15, 比例=16:9, 首帧参考图必填 |

---

## 六、扩展新模型的思想

```
┌─────────────────────────────────────────────────────────────┐
│                    新模型接入流程                            │
├─────────────────────────────────────────────────────────────┤
│  1. 前端保持统一参数（resolution/duration/mode...）          │
│  2. 在 video_param_mapper.js 添加新模型映射规则              │
│  3. 定义该模型不支持的字段 → 自动 delete 剥离                │
│  4. 定义该模型专属字段 → 自动转换命名                      │
└────────────────────────────���─��──────────────────────────────┘
```

> 💡 **核心优势**：前端不用改，用户无感知，新模型接入只需更新映射脚本！

---

## 七、更新日志

| 版本 | 更新内容 | 状态 |
|------|---------|------|
| v1.0 | Veo 系列参数映射 | ✅ 已完成 |
| v1.1 | Grok 系列参数映射 + 老版兼容 | ✅ 已完成 |
| v1.2 | 自动 delete 不兼容字段逻辑 | ✅ 已完成 |
| v1.3 | 音效参数 generate_audio 映射 | ✅ 已完成 |

---